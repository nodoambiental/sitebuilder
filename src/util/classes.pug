mixin placeholderSoPugReadsThisShiButThatActuallyIsTheIdentityMixinIGuess(potat)
    = `${potat}`



-
    const CSS = (className) => {
        const daisy = {
            nav: "navbar bg-base-100",
            navButton: "btn btn-ghost normal-case text-xl"
        }
        return daisy[className]
    }